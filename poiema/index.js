// index.js
// my first .js file

const arr = [1, 2, 3];

arr.forEach(console.log);

// 에러 발생
// alert 함수는 클라이언트 사이트 Web API
// 코드가 브라우저가 아닌 Node.js 환경에서 실행된 것이기 때문

// const arr = [1, 2, 3];
// arr.forEach(alert);